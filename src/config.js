/** @module config */

/**
 * The Mud server's configuration
 * @example
 * const config = Object.freeze({
 *   port: 8080 // Port to serve the Mud on
 * });
 */
const config = Object.freeze({
  port: 8080
});

export { config };
